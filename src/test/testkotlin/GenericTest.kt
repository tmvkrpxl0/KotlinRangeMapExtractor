import java.io.Serializable as S
import java.lang.Runnable as R

fun test(a: Int): IntArray {
    when(val b = 2f) {
        is Number -> {

        }
    }
    val c = "asdf"
    return intArrayOf(1, 2, 3)
}